#lang scheme
(require rackunit)



(define (f n)
  (cond
    ((< n 3) n)
    (else (+ (f (- n 1))
             (f (- n 2))
             (f (- n 3))))))

(check-eq? (f 0) 0)
(check-eq? (f 1) 1)
(check-eq? (f 2) 2)
(check-eq? (f 3) 4)
(check-eq? (f 4) 11)